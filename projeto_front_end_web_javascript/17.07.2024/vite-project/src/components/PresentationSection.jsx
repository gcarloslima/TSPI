function PresentationSection({title, subtitle}) {
    return (
        <div className="presentation">
            <h2>{title}</h2>
            <p>{subtitle}</p>
        </div>
    )
}

export default PresentationSection
